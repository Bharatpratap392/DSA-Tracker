//Check if a given year is a leap year
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin>>n;
    if(n%4==0){
        cout<<"it's a leap year";
    }
    else{
        cout<<"it's not a leap year";
    }
    return 0;
}